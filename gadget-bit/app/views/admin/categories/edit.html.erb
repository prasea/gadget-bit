<div class="container-md mx-auto">
  <h1 class="fw-bold fs-4">Editing category</h1>
  <%= render 'form', category: @category%>
  <div class="mt-3">
    <%= link_to "Show this category", admin_category_path(@category), class: "btn btn-secondary me-2 fw-medium" %>
    <%= link_to "Back to categories", admin_categories_path, class: "btn btn-secondary fw-medium" %>
  </div>
</div>
